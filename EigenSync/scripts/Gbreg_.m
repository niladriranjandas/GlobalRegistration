
clc; clear all;

calc_rot_corr;

vertices=combinePatch(Patch);

m=length(vertices);
n=length(Patch);

tau=zeros(m,n);

for i= 1:(n-1)
    for j= 1:(n-1)

        if length(pairs{i,j}>1)
            
            
        else
            
            
        end
        
    end
end
